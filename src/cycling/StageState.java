package cycling;

public enum StageState {

    /**
     * Stage is waiting for results.
     */
    WAITING,

    /**
     * Stage is not waiting for results (i.e., can be edited).
     */
    IDLE

}
